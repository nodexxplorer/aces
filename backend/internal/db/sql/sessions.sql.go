// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: sessions.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createSession = `-- name: CreateSession :one
INSERT INTO sessions (
    name, start_date, end_date
) VALUES (
    $1, $2, $3
) RETURNING id, name, start_date, end_date, is_active, is_archived, created_at
`

type CreateSessionParams struct {
	Name      string             `json:"name"`
	StartDate pgtype.Timestamptz `json:"start_date"`
	EndDate   pgtype.Timestamptz `json:"end_date"`
}

func (q *Queries) CreateSession(ctx context.Context, arg CreateSessionParams) (Session, error) {
	row := q.db.QueryRow(ctx, createSession, arg.Name, arg.StartDate, arg.EndDate)
	var i Session
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.StartDate,
		&i.EndDate,
		&i.IsActive,
		&i.IsArchived,
		&i.CreatedAt,
	)
	return i, err
}

const deleteSession = `-- name: DeleteSession :exec
DELETE FROM sessions
WHERE id = $1
`

func (q *Queries) DeleteSession(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteSession, id)
	return err
}

const getActiveSession = `-- name: GetActiveSession :one
SELECT id, name, start_date, end_date, is_active, is_archived, created_at FROM sessions
WHERE is_active = true LIMIT 1
`

func (q *Queries) GetActiveSession(ctx context.Context) (Session, error) {
	row := q.db.QueryRow(ctx, getActiveSession)
	var i Session
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.StartDate,
		&i.EndDate,
		&i.IsActive,
		&i.IsArchived,
		&i.CreatedAt,
	)
	return i, err
}

const getSession = `-- name: GetSession :one
SELECT id, name, start_date, end_date, is_active, is_archived, created_at FROM sessions
WHERE id = $1 LIMIT 1
`

func (q *Queries) GetSession(ctx context.Context, id uuid.UUID) (Session, error) {
	row := q.db.QueryRow(ctx, getSession, id)
	var i Session
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.StartDate,
		&i.EndDate,
		&i.IsActive,
		&i.IsArchived,
		&i.CreatedAt,
	)
	return i, err
}

const getSessionByName = `-- name: GetSessionByName :one
SELECT id, name, start_date, end_date, is_active, is_archived, created_at FROM sessions
WHERE name = $1 LIMIT 1
`

func (q *Queries) GetSessionByName(ctx context.Context, name string) (Session, error) {
	row := q.db.QueryRow(ctx, getSessionByName, name)
	var i Session
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.StartDate,
		&i.EndDate,
		&i.IsActive,
		&i.IsArchived,
		&i.CreatedAt,
	)
	return i, err
}

const listSessions = `-- name: ListSessions :many
SELECT id, name, start_date, end_date, is_active, is_archived, created_at FROM sessions
ORDER BY start_date DESC
LIMIT $1 OFFSET $2
`

type ListSessionsParams struct {
	Limit  int32 `json:"limit"`
	Offset int32 `json:"offset"`
}

func (q *Queries) ListSessions(ctx context.Context, arg ListSessionsParams) ([]Session, error) {
	rows, err := q.db.Query(ctx, listSessions, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Session{}
	for rows.Next() {
		var i Session
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.StartDate,
			&i.EndDate,
			&i.IsActive,
			&i.IsArchived,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateSession = `-- name: UpdateSession :one
UPDATE sessions
SET
    name = $2,
    start_date = $3,
    end_date = $4,
    is_active = $5,
    is_archived = $6
WHERE id = $1
RETURNING id, name, start_date, end_date, is_active, is_archived, created_at
`

type UpdateSessionParams struct {
	ID         uuid.UUID          `json:"id"`
	Name       string             `json:"name"`
	StartDate  pgtype.Timestamptz `json:"start_date"`
	EndDate    pgtype.Timestamptz `json:"end_date"`
	IsActive   bool               `json:"is_active"`
	IsArchived bool               `json:"is_archived"`
}

func (q *Queries) UpdateSession(ctx context.Context, arg UpdateSessionParams) (Session, error) {
	row := q.db.QueryRow(ctx, updateSession,
		arg.ID,
		arg.Name,
		arg.StartDate,
		arg.EndDate,
		arg.IsActive,
		arg.IsArchived,
	)
	var i Session
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.StartDate,
		&i.EndDate,
		&i.IsActive,
		&i.IsArchived,
		&i.CreatedAt,
	)
	return i, err
}
