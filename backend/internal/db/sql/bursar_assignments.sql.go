// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: bursar_assignments.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createBursarAssignment = `-- name: CreateBursarAssignment :one

INSERT INTO bursar_assignments (
    user_id, level, bursar_type, session_id, assigned_by
) VALUES (
    $1, $2, $3, $4, $5
) RETURNING id, user_id, level, bursar_type, session_id, assigned_by, is_active, assigned_at, revoked_at
`

type CreateBursarAssignmentParams struct {
	UserID     uuid.UUID   `json:"user_id"`
	Level      int32       `json:"level"`
	BursarType string      `json:"bursar_type"`
	SessionID  pgtype.UUID `json:"session_id"`
	AssignedBy uuid.UUID   `json:"assigned_by"`
}

// ==================== BURSAR ASSIGNMENTS ====================
func (q *Queries) CreateBursarAssignment(ctx context.Context, arg CreateBursarAssignmentParams) (BursarAssignment, error) {
	row := q.db.QueryRow(ctx, createBursarAssignment,
		arg.UserID,
		arg.Level,
		arg.BursarType,
		arg.SessionID,
		arg.AssignedBy,
	)
	var i BursarAssignment
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Level,
		&i.BursarType,
		&i.SessionID,
		&i.AssignedBy,
		&i.IsActive,
		&i.AssignedAt,
		&i.RevokedAt,
	)
	return i, err
}

const getBursarAssignment = `-- name: GetBursarAssignment :one
SELECT id, user_id, level, bursar_type, session_id, assigned_by, is_active, assigned_at, revoked_at FROM bursar_assignments
WHERE id = $1 LIMIT 1
`

func (q *Queries) GetBursarAssignment(ctx context.Context, id uuid.UUID) (BursarAssignment, error) {
	row := q.db.QueryRow(ctx, getBursarAssignment, id)
	var i BursarAssignment
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Level,
		&i.BursarType,
		&i.SessionID,
		&i.AssignedBy,
		&i.IsActive,
		&i.AssignedAt,
		&i.RevokedAt,
	)
	return i, err
}

const listActiveBursarAssignments = `-- name: ListActiveBursarAssignments :many
SELECT id, user_id, level, bursar_type, session_id, assigned_by, is_active, assigned_at, revoked_at FROM bursar_assignments
WHERE is_active = true
ORDER BY level, bursar_type
`

func (q *Queries) ListActiveBursarAssignments(ctx context.Context) ([]BursarAssignment, error) {
	rows, err := q.db.Query(ctx, listActiveBursarAssignments)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []BursarAssignment{}
	for rows.Next() {
		var i BursarAssignment
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Level,
			&i.BursarType,
			&i.SessionID,
			&i.AssignedBy,
			&i.IsActive,
			&i.AssignedAt,
			&i.RevokedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listBursarAssignmentsByUser = `-- name: ListBursarAssignmentsByUser :many
SELECT id, user_id, level, bursar_type, session_id, assigned_by, is_active, assigned_at, revoked_at FROM bursar_assignments
WHERE user_id = $1 AND is_active = true
`

func (q *Queries) ListBursarAssignmentsByUser(ctx context.Context, userID uuid.UUID) ([]BursarAssignment, error) {
	rows, err := q.db.Query(ctx, listBursarAssignmentsByUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []BursarAssignment{}
	for rows.Next() {
		var i BursarAssignment
		if err := rows.Scan(
			&i.ID,
			&i.UserID,
			&i.Level,
			&i.BursarType,
			&i.SessionID,
			&i.AssignedBy,
			&i.IsActive,
			&i.AssignedAt,
			&i.RevokedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const revokeBursarAssignment = `-- name: RevokeBursarAssignment :one
UPDATE bursar_assignments
SET is_active = false, revoked_at = NOW()
WHERE id = $1
RETURNING id, user_id, level, bursar_type, session_id, assigned_by, is_active, assigned_at, revoked_at
`

func (q *Queries) RevokeBursarAssignment(ctx context.Context, id uuid.UUID) (BursarAssignment, error) {
	row := q.db.QueryRow(ctx, revokeBursarAssignment, id)
	var i BursarAssignment
	err := row.Scan(
		&i.ID,
		&i.UserID,
		&i.Level,
		&i.BursarType,
		&i.SessionID,
		&i.AssignedBy,
		&i.IsActive,
		&i.AssignedAt,
		&i.RevokedAt,
	)
	return i, err
}
