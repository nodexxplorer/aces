// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: campus_connect.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const addGroupMember = `-- name: AddGroupMember :one

INSERT INTO group_members (
    group_id, user_id, role
) VALUES (
    $1, $2, $3
) RETURNING id, group_id, user_id, role, joined_at
`

type AddGroupMemberParams struct {
	GroupID uuid.UUID `json:"group_id"`
	UserID  uuid.UUID `json:"user_id"`
	Role    string    `json:"role"`
}

// ==================== GROUP MEMBERS ====================
func (q *Queries) AddGroupMember(ctx context.Context, arg AddGroupMemberParams) (GroupMember, error) {
	row := q.db.QueryRow(ctx, addGroupMember, arg.GroupID, arg.UserID, arg.Role)
	var i GroupMember
	err := row.Scan(
		&i.ID,
		&i.GroupID,
		&i.UserID,
		&i.Role,
		&i.JoinedAt,
	)
	return i, err
}

const checkConnectionExists = `-- name: CheckConnectionExists :one
SELECT EXISTS(
    SELECT 1 FROM connections
    WHERE (requester_id = $1 AND receiver_id = $2)
       OR (requester_id = $2 AND receiver_id = $1)
) AS connection_exists
`

type CheckConnectionExistsParams struct {
	RequesterID uuid.UUID `json:"requester_id"`
	ReceiverID  uuid.UUID `json:"receiver_id"`
}

func (q *Queries) CheckConnectionExists(ctx context.Context, arg CheckConnectionExistsParams) (bool, error) {
	row := q.db.QueryRow(ctx, checkConnectionExists, arg.RequesterID, arg.ReceiverID)
	var connection_exists bool
	err := row.Scan(&connection_exists)
	return connection_exists, err
}

const checkGroupMembership = `-- name: CheckGroupMembership :one
SELECT EXISTS(
    SELECT 1 FROM group_members
    WHERE group_id = $1 AND user_id = $2
) AS is_member
`

type CheckGroupMembershipParams struct {
	GroupID uuid.UUID `json:"group_id"`
	UserID  uuid.UUID `json:"user_id"`
}

func (q *Queries) CheckGroupMembership(ctx context.Context, arg CheckGroupMembershipParams) (bool, error) {
	row := q.db.QueryRow(ctx, checkGroupMembership, arg.GroupID, arg.UserID)
	var is_member bool
	err := row.Scan(&is_member)
	return is_member, err
}

const countUnreadMessages = `-- name: CountUnreadMessages :one
SELECT COUNT(*) AS unread_count
FROM messages
WHERE receiver_id = $1 AND is_read = false
`

func (q *Queries) CountUnreadMessages(ctx context.Context, receiverID uuid.UUID) (int64, error) {
	row := q.db.QueryRow(ctx, countUnreadMessages, receiverID)
	var unread_count int64
	err := row.Scan(&unread_count)
	return unread_count, err
}

const createConnection = `-- name: CreateConnection :one

INSERT INTO connections (
    requester_id, receiver_id, message
) VALUES (
    $1, $2, $3
) RETURNING id, requester_id, receiver_id, status, message, responded_at, created_at
`

type CreateConnectionParams struct {
	RequesterID uuid.UUID `json:"requester_id"`
	ReceiverID  uuid.UUID `json:"receiver_id"`
	Message     *string   `json:"message"`
}

// ==================== CONNECTIONS ====================
func (q *Queries) CreateConnection(ctx context.Context, arg CreateConnectionParams) (Connection, error) {
	row := q.db.QueryRow(ctx, createConnection, arg.RequesterID, arg.ReceiverID, arg.Message)
	var i Connection
	err := row.Scan(
		&i.ID,
		&i.RequesterID,
		&i.ReceiverID,
		&i.Status,
		&i.Message,
		&i.RespondedAt,
		&i.CreatedAt,
	)
	return i, err
}

const createGroup = `-- name: CreateGroup :one

INSERT INTO groups (
    name, description, category, avatar_url, max_members, is_private, created_by
) VALUES (
    $1, $2, $3, $4, $5, $6, $7
) RETURNING id, name, description, category, avatar_url, max_members, is_private, created_by, created_at, updated_at
`

type CreateGroupParams struct {
	Name        string    `json:"name"`
	Description *string   `json:"description"`
	Category    string    `json:"category"`
	AvatarUrl   *string   `json:"avatar_url"`
	MaxMembers  *int32    `json:"max_members"`
	IsPrivate   bool      `json:"is_private"`
	CreatedBy   uuid.UUID `json:"created_by"`
}

// ==================== GROUPS ====================
func (q *Queries) CreateGroup(ctx context.Context, arg CreateGroupParams) (Group, error) {
	row := q.db.QueryRow(ctx, createGroup,
		arg.Name,
		arg.Description,
		arg.Category,
		arg.AvatarUrl,
		arg.MaxMembers,
		arg.IsPrivate,
		arg.CreatedBy,
	)
	var i Group
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.Category,
		&i.AvatarUrl,
		&i.MaxMembers,
		&i.IsPrivate,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const createGroupMessage = `-- name: CreateGroupMessage :one

INSERT INTO group_messages (
    group_id, sender_id, content
) VALUES (
    $1, $2, $3
) RETURNING id, group_id, sender_id, content, created_at
`

type CreateGroupMessageParams struct {
	GroupID  uuid.UUID `json:"group_id"`
	SenderID uuid.UUID `json:"sender_id"`
	Content  string    `json:"content"`
}

// ==================== GROUP MESSAGES ====================
func (q *Queries) CreateGroupMessage(ctx context.Context, arg CreateGroupMessageParams) (GroupMessage, error) {
	row := q.db.QueryRow(ctx, createGroupMessage, arg.GroupID, arg.SenderID, arg.Content)
	var i GroupMessage
	err := row.Scan(
		&i.ID,
		&i.GroupID,
		&i.SenderID,
		&i.Content,
		&i.CreatedAt,
	)
	return i, err
}

const createMessage = `-- name: CreateMessage :one

INSERT INTO messages (
    sender_id, receiver_id, content
) VALUES (
    $1, $2, $3
) RETURNING id, sender_id, receiver_id, content, is_read, read_at, created_at
`

type CreateMessageParams struct {
	SenderID   uuid.UUID `json:"sender_id"`
	ReceiverID uuid.UUID `json:"receiver_id"`
	Content    string    `json:"content"`
}

// ==================== MESSAGES ====================
func (q *Queries) CreateMessage(ctx context.Context, arg CreateMessageParams) (Message, error) {
	row := q.db.QueryRow(ctx, createMessage, arg.SenderID, arg.ReceiverID, arg.Content)
	var i Message
	err := row.Scan(
		&i.ID,
		&i.SenderID,
		&i.ReceiverID,
		&i.Content,
		&i.IsRead,
		&i.ReadAt,
		&i.CreatedAt,
	)
	return i, err
}

const deleteGroup = `-- name: DeleteGroup :exec
DELETE FROM groups WHERE id = $1
`

func (q *Queries) DeleteGroup(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteGroup, id)
	return err
}

const getAlumniDirectory = `-- name: GetAlumniDirectory :many
SELECT u.id, u.full_name, u.avatar_url, u.email,
       als.graduation_year, als.current_company, als.current_position, als.is_mentor_available
FROM users u
JOIN alumni_status als ON u.id = als.user_id
WHERE u.is_active = true AND als.verification_status = 'verified'
ORDER BY u.full_name
LIMIT $1 OFFSET $2
`

type GetAlumniDirectoryParams struct {
	Limit  int32 `json:"limit"`
	Offset int32 `json:"offset"`
}

type GetAlumniDirectoryRow struct {
	ID                uuid.UUID `json:"id"`
	FullName          string    `json:"full_name"`
	AvatarUrl         *string   `json:"avatar_url"`
	Email             string    `json:"email"`
	GraduationYear    int32     `json:"graduation_year"`
	CurrentCompany    *string   `json:"current_company"`
	CurrentPosition   *string   `json:"current_position"`
	IsMentorAvailable bool      `json:"is_mentor_available"`
}

func (q *Queries) GetAlumniDirectory(ctx context.Context, arg GetAlumniDirectoryParams) ([]GetAlumniDirectoryRow, error) {
	rows, err := q.db.Query(ctx, getAlumniDirectory, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetAlumniDirectoryRow{}
	for rows.Next() {
		var i GetAlumniDirectoryRow
		if err := rows.Scan(
			&i.ID,
			&i.FullName,
			&i.AvatarUrl,
			&i.Email,
			&i.GraduationYear,
			&i.CurrentCompany,
			&i.CurrentPosition,
			&i.IsMentorAvailable,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getConnection = `-- name: GetConnection :one
SELECT id, requester_id, receiver_id, status, message, responded_at, created_at FROM connections
WHERE id = $1 LIMIT 1
`

func (q *Queries) GetConnection(ctx context.Context, id uuid.UUID) (Connection, error) {
	row := q.db.QueryRow(ctx, getConnection, id)
	var i Connection
	err := row.Scan(
		&i.ID,
		&i.RequesterID,
		&i.ReceiverID,
		&i.Status,
		&i.Message,
		&i.RespondedAt,
		&i.CreatedAt,
	)
	return i, err
}

const getGroup = `-- name: GetGroup :one
SELECT id, name, description, category, avatar_url, max_members, is_private, created_by, created_at, updated_at FROM groups
WHERE id = $1 LIMIT 1
`

func (q *Queries) GetGroup(ctx context.Context, id uuid.UUID) (Group, error) {
	row := q.db.QueryRow(ctx, getGroup, id)
	var i Group
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.Category,
		&i.AvatarUrl,
		&i.MaxMembers,
		&i.IsPrivate,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getStudentDirectory = `-- name: GetStudentDirectory :many
SELECT u.id, u.full_name, u.avatar_url, u.email, s.matric_number, s.level
FROM users u
JOIN students s ON u.id = s.user_id
WHERE u.is_active = true AND u.is_approved = true
ORDER BY u.full_name
LIMIT $1 OFFSET $2
`

type GetStudentDirectoryParams struct {
	Limit  int32 `json:"limit"`
	Offset int32 `json:"offset"`
}

type GetStudentDirectoryRow struct {
	ID           uuid.UUID `json:"id"`
	FullName     string    `json:"full_name"`
	AvatarUrl    *string   `json:"avatar_url"`
	Email        string    `json:"email"`
	MatricNumber string    `json:"matric_number"`
	Level        int32     `json:"level"`
}

func (q *Queries) GetStudentDirectory(ctx context.Context, arg GetStudentDirectoryParams) ([]GetStudentDirectoryRow, error) {
	rows, err := q.db.Query(ctx, getStudentDirectory, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []GetStudentDirectoryRow{}
	for rows.Next() {
		var i GetStudentDirectoryRow
		if err := rows.Scan(
			&i.ID,
			&i.FullName,
			&i.AvatarUrl,
			&i.Email,
			&i.MatricNumber,
			&i.Level,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listConversation = `-- name: ListConversation :many
SELECT id, sender_id, receiver_id, content, is_read, read_at, created_at FROM messages
WHERE (sender_id = $1 AND receiver_id = $2)
   OR (sender_id = $2 AND receiver_id = $1)
ORDER BY created_at ASC
LIMIT $3 OFFSET $4
`

type ListConversationParams struct {
	SenderID   uuid.UUID `json:"sender_id"`
	ReceiverID uuid.UUID `json:"receiver_id"`
	Limit      int32     `json:"limit"`
	Offset     int32     `json:"offset"`
}

func (q *Queries) ListConversation(ctx context.Context, arg ListConversationParams) ([]Message, error) {
	rows, err := q.db.Query(ctx, listConversation,
		arg.SenderID,
		arg.ReceiverID,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Message{}
	for rows.Next() {
		var i Message
		if err := rows.Scan(
			&i.ID,
			&i.SenderID,
			&i.ReceiverID,
			&i.Content,
			&i.IsRead,
			&i.ReadAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listGroupMembers = `-- name: ListGroupMembers :many
SELECT gm.id, gm.group_id, gm.user_id, gm.role, gm.joined_at, u.full_name, u.avatar_url, u.email
FROM group_members gm
JOIN users u ON gm.user_id = u.id
WHERE gm.group_id = $1
ORDER BY gm.role, u.full_name
`

type ListGroupMembersRow struct {
	ID        uuid.UUID          `json:"id"`
	GroupID   uuid.UUID          `json:"group_id"`
	UserID    uuid.UUID          `json:"user_id"`
	Role      string             `json:"role"`
	JoinedAt  pgtype.Timestamptz `json:"joined_at"`
	FullName  string             `json:"full_name"`
	AvatarUrl *string            `json:"avatar_url"`
	Email     string             `json:"email"`
}

func (q *Queries) ListGroupMembers(ctx context.Context, groupID uuid.UUID) ([]ListGroupMembersRow, error) {
	rows, err := q.db.Query(ctx, listGroupMembers, groupID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListGroupMembersRow{}
	for rows.Next() {
		var i ListGroupMembersRow
		if err := rows.Scan(
			&i.ID,
			&i.GroupID,
			&i.UserID,
			&i.Role,
			&i.JoinedAt,
			&i.FullName,
			&i.AvatarUrl,
			&i.Email,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listGroupMessages = `-- name: ListGroupMessages :many
SELECT gm.id, gm.group_id, gm.sender_id, gm.content, gm.created_at, u.full_name, u.avatar_url
FROM group_messages gm
JOIN users u ON gm.sender_id = u.id
WHERE gm.group_id = $1
ORDER BY gm.created_at ASC
LIMIT $2 OFFSET $3
`

type ListGroupMessagesParams struct {
	GroupID uuid.UUID `json:"group_id"`
	Limit   int32     `json:"limit"`
	Offset  int32     `json:"offset"`
}

type ListGroupMessagesRow struct {
	ID        uuid.UUID          `json:"id"`
	GroupID   uuid.UUID          `json:"group_id"`
	SenderID  uuid.UUID          `json:"sender_id"`
	Content   string             `json:"content"`
	CreatedAt pgtype.Timestamptz `json:"created_at"`
	FullName  string             `json:"full_name"`
	AvatarUrl *string            `json:"avatar_url"`
}

func (q *Queries) ListGroupMessages(ctx context.Context, arg ListGroupMessagesParams) ([]ListGroupMessagesRow, error) {
	rows, err := q.db.Query(ctx, listGroupMessages, arg.GroupID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListGroupMessagesRow{}
	for rows.Next() {
		var i ListGroupMessagesRow
		if err := rows.Scan(
			&i.ID,
			&i.GroupID,
			&i.SenderID,
			&i.Content,
			&i.CreatedAt,
			&i.FullName,
			&i.AvatarUrl,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listGroups = `-- name: ListGroups :many
SELECT g.id, g.name, g.description, g.category, g.avatar_url, g.max_members, g.is_private, g.created_by, g.created_at, g.updated_at, COUNT(gm.id) AS member_count
FROM groups g
LEFT JOIN group_members gm ON g.id = gm.group_id
WHERE g.is_private = false
GROUP BY g.id
ORDER BY g.created_at DESC
LIMIT $1 OFFSET $2
`

type ListGroupsParams struct {
	Limit  int32 `json:"limit"`
	Offset int32 `json:"offset"`
}

type ListGroupsRow struct {
	ID          uuid.UUID          `json:"id"`
	Name        string             `json:"name"`
	Description *string            `json:"description"`
	Category    string             `json:"category"`
	AvatarUrl   *string            `json:"avatar_url"`
	MaxMembers  *int32             `json:"max_members"`
	IsPrivate   bool               `json:"is_private"`
	CreatedBy   uuid.UUID          `json:"created_by"`
	CreatedAt   pgtype.Timestamptz `json:"created_at"`
	UpdatedAt   pgtype.Timestamptz `json:"updated_at"`
	MemberCount int64              `json:"member_count"`
}

func (q *Queries) ListGroups(ctx context.Context, arg ListGroupsParams) ([]ListGroupsRow, error) {
	rows, err := q.db.Query(ctx, listGroups, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListGroupsRow{}
	for rows.Next() {
		var i ListGroupsRow
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Description,
			&i.Category,
			&i.AvatarUrl,
			&i.MaxMembers,
			&i.IsPrivate,
			&i.CreatedBy,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.MemberCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listPendingConnectionRequests = `-- name: ListPendingConnectionRequests :many
SELECT c.id, c.requester_id, c.receiver_id, c.status, c.message, c.responded_at, c.created_at, u.full_name, u.avatar_url, u.role
FROM connections c
JOIN users u ON c.requester_id = u.id
WHERE c.receiver_id = $1 AND c.status = 'pending'
ORDER BY c.created_at DESC
`

type ListPendingConnectionRequestsRow struct {
	ID          uuid.UUID          `json:"id"`
	RequesterID uuid.UUID          `json:"requester_id"`
	ReceiverID  uuid.UUID          `json:"receiver_id"`
	Status      ConnectionStatus   `json:"status"`
	Message     *string            `json:"message"`
	RespondedAt pgtype.Timestamptz `json:"responded_at"`
	CreatedAt   pgtype.Timestamptz `json:"created_at"`
	FullName    string             `json:"full_name"`
	AvatarUrl   *string            `json:"avatar_url"`
	Role        UserRole           `json:"role"`
}

func (q *Queries) ListPendingConnectionRequests(ctx context.Context, receiverID uuid.UUID) ([]ListPendingConnectionRequestsRow, error) {
	rows, err := q.db.Query(ctx, listPendingConnectionRequests, receiverID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListPendingConnectionRequestsRow{}
	for rows.Next() {
		var i ListPendingConnectionRequestsRow
		if err := rows.Scan(
			&i.ID,
			&i.RequesterID,
			&i.ReceiverID,
			&i.Status,
			&i.Message,
			&i.RespondedAt,
			&i.CreatedAt,
			&i.FullName,
			&i.AvatarUrl,
			&i.Role,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listUserConnections = `-- name: ListUserConnections :many
SELECT c.id, c.requester_id, c.receiver_id, c.status, c.message, c.responded_at, c.created_at, u.full_name, u.avatar_url, u.role
FROM connections c
JOIN users u ON (
    CASE WHEN c.requester_id = $1 THEN c.receiver_id ELSE c.requester_id END
) = u.id
WHERE (c.requester_id = $1 OR c.receiver_id = $1) AND c.status = 'accepted'
ORDER BY c.created_at DESC
LIMIT $2 OFFSET $3
`

type ListUserConnectionsParams struct {
	RequesterID uuid.UUID `json:"requester_id"`
	Limit       int32     `json:"limit"`
	Offset      int32     `json:"offset"`
}

type ListUserConnectionsRow struct {
	ID          uuid.UUID          `json:"id"`
	RequesterID uuid.UUID          `json:"requester_id"`
	ReceiverID  uuid.UUID          `json:"receiver_id"`
	Status      ConnectionStatus   `json:"status"`
	Message     *string            `json:"message"`
	RespondedAt pgtype.Timestamptz `json:"responded_at"`
	CreatedAt   pgtype.Timestamptz `json:"created_at"`
	FullName    string             `json:"full_name"`
	AvatarUrl   *string            `json:"avatar_url"`
	Role        UserRole           `json:"role"`
}

func (q *Queries) ListUserConnections(ctx context.Context, arg ListUserConnectionsParams) ([]ListUserConnectionsRow, error) {
	rows, err := q.db.Query(ctx, listUserConnections, arg.RequesterID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListUserConnectionsRow{}
	for rows.Next() {
		var i ListUserConnectionsRow
		if err := rows.Scan(
			&i.ID,
			&i.RequesterID,
			&i.ReceiverID,
			&i.Status,
			&i.Message,
			&i.RespondedAt,
			&i.CreatedAt,
			&i.FullName,
			&i.AvatarUrl,
			&i.Role,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listUserGroups = `-- name: ListUserGroups :many
SELECT g.id, g.name, g.description, g.category, g.avatar_url, g.max_members, g.is_private, g.created_by, g.created_at, g.updated_at, gm.role AS member_role
FROM groups g
JOIN group_members gm ON g.id = gm.group_id
WHERE gm.user_id = $1
ORDER BY g.name
`

type ListUserGroupsRow struct {
	ID          uuid.UUID          `json:"id"`
	Name        string             `json:"name"`
	Description *string            `json:"description"`
	Category    string             `json:"category"`
	AvatarUrl   *string            `json:"avatar_url"`
	MaxMembers  *int32             `json:"max_members"`
	IsPrivate   bool               `json:"is_private"`
	CreatedBy   uuid.UUID          `json:"created_by"`
	CreatedAt   pgtype.Timestamptz `json:"created_at"`
	UpdatedAt   pgtype.Timestamptz `json:"updated_at"`
	MemberRole  string             `json:"member_role"`
}

func (q *Queries) ListUserGroups(ctx context.Context, userID uuid.UUID) ([]ListUserGroupsRow, error) {
	rows, err := q.db.Query(ctx, listUserGroups, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListUserGroupsRow{}
	for rows.Next() {
		var i ListUserGroupsRow
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Description,
			&i.Category,
			&i.AvatarUrl,
			&i.MaxMembers,
			&i.IsPrivate,
			&i.CreatedBy,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.MemberRole,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const markMessageRead = `-- name: MarkMessageRead :one
UPDATE messages
SET is_read = true, read_at = NOW()
WHERE id = $1
RETURNING id, sender_id, receiver_id, content, is_read, read_at, created_at
`

func (q *Queries) MarkMessageRead(ctx context.Context, id uuid.UUID) (Message, error) {
	row := q.db.QueryRow(ctx, markMessageRead, id)
	var i Message
	err := row.Scan(
		&i.ID,
		&i.SenderID,
		&i.ReceiverID,
		&i.Content,
		&i.IsRead,
		&i.ReadAt,
		&i.CreatedAt,
	)
	return i, err
}

const removeGroupMember = `-- name: RemoveGroupMember :exec
DELETE FROM group_members
WHERE group_id = $1 AND user_id = $2
`

type RemoveGroupMemberParams struct {
	GroupID uuid.UUID `json:"group_id"`
	UserID  uuid.UUID `json:"user_id"`
}

func (q *Queries) RemoveGroupMember(ctx context.Context, arg RemoveGroupMemberParams) error {
	_, err := q.db.Exec(ctx, removeGroupMember, arg.GroupID, arg.UserID)
	return err
}

const updateConnectionStatus = `-- name: UpdateConnectionStatus :one
UPDATE connections
SET status = $2, responded_at = NOW()
WHERE id = $1
RETURNING id, requester_id, receiver_id, status, message, responded_at, created_at
`

type UpdateConnectionStatusParams struct {
	ID     uuid.UUID        `json:"id"`
	Status ConnectionStatus `json:"status"`
}

func (q *Queries) UpdateConnectionStatus(ctx context.Context, arg UpdateConnectionStatusParams) (Connection, error) {
	row := q.db.QueryRow(ctx, updateConnectionStatus, arg.ID, arg.Status)
	var i Connection
	err := row.Scan(
		&i.ID,
		&i.RequesterID,
		&i.ReceiverID,
		&i.Status,
		&i.Message,
		&i.RespondedAt,
		&i.CreatedAt,
	)
	return i, err
}

const updateGroup = `-- name: UpdateGroup :one
UPDATE groups
SET name = $2, description = $3, category = $4, avatar_url = $5,
    max_members = $6, is_private = $7, updated_at = NOW()
WHERE id = $1
RETURNING id, name, description, category, avatar_url, max_members, is_private, created_by, created_at, updated_at
`

type UpdateGroupParams struct {
	ID          uuid.UUID `json:"id"`
	Name        string    `json:"name"`
	Description *string   `json:"description"`
	Category    string    `json:"category"`
	AvatarUrl   *string   `json:"avatar_url"`
	MaxMembers  *int32    `json:"max_members"`
	IsPrivate   bool      `json:"is_private"`
}

func (q *Queries) UpdateGroup(ctx context.Context, arg UpdateGroupParams) (Group, error) {
	row := q.db.QueryRow(ctx, updateGroup,
		arg.ID,
		arg.Name,
		arg.Description,
		arg.Category,
		arg.AvatarUrl,
		arg.MaxMembers,
		arg.IsPrivate,
	)
	var i Group
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.Category,
		&i.AvatarUrl,
		&i.MaxMembers,
		&i.IsPrivate,
		&i.CreatedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateGroupMemberRole = `-- name: UpdateGroupMemberRole :one
UPDATE group_members
SET role = $3
WHERE group_id = $1 AND user_id = $2
RETURNING id, group_id, user_id, role, joined_at
`

type UpdateGroupMemberRoleParams struct {
	GroupID uuid.UUID `json:"group_id"`
	UserID  uuid.UUID `json:"user_id"`
	Role    string    `json:"role"`
}

func (q *Queries) UpdateGroupMemberRole(ctx context.Context, arg UpdateGroupMemberRoleParams) (GroupMember, error) {
	row := q.db.QueryRow(ctx, updateGroupMemberRole, arg.GroupID, arg.UserID, arg.Role)
	var i GroupMember
	err := row.Scan(
		&i.ID,
		&i.GroupID,
		&i.UserID,
		&i.Role,
		&i.JoinedAt,
	)
	return i, err
}
