// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: complaints.sql

package db

import (
	"context"

	"github.com/google/uuid"
)

const createComplaint = `-- name: CreateComplaint :one
INSERT INTO complaints (
    student_id, category, subject, body, priority, status
) VALUES (
    $1, $2, $3, $4, $5, $6
) RETURNING id, student_id, category, subject, body, priority, status, assigned_to, resolution, resolved_at, resolved_by, created_at, updated_at
`

type CreateComplaintParams struct {
	StudentID uuid.UUID         `json:"student_id"`
	Category  ComplaintCategory `json:"category"`
	Subject   string            `json:"subject"`
	Body      string            `json:"body"`
	Priority  ComplaintPriority `json:"priority"`
	Status    ComplaintStatus   `json:"status"`
}

func (q *Queries) CreateComplaint(ctx context.Context, arg CreateComplaintParams) (Complaint, error) {
	row := q.db.QueryRow(ctx, createComplaint,
		arg.StudentID,
		arg.Category,
		arg.Subject,
		arg.Body,
		arg.Priority,
		arg.Status,
	)
	var i Complaint
	err := row.Scan(
		&i.ID,
		&i.StudentID,
		&i.Category,
		&i.Subject,
		&i.Body,
		&i.Priority,
		&i.Status,
		&i.AssignedTo,
		&i.Resolution,
		&i.ResolvedAt,
		&i.ResolvedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteComplaint = `-- name: DeleteComplaint :exec
DELETE FROM complaints
WHERE id = $1
`

func (q *Queries) DeleteComplaint(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, deleteComplaint, id)
	return err
}

const getComplaint = `-- name: GetComplaint :one
SELECT id, student_id, category, subject, body, priority, status, assigned_to, resolution, resolved_at, resolved_by, created_at, updated_at FROM complaints
WHERE id = $1 LIMIT 1
`

func (q *Queries) GetComplaint(ctx context.Context, id uuid.UUID) (Complaint, error) {
	row := q.db.QueryRow(ctx, getComplaint, id)
	var i Complaint
	err := row.Scan(
		&i.ID,
		&i.StudentID,
		&i.Category,
		&i.Subject,
		&i.Body,
		&i.Priority,
		&i.Status,
		&i.AssignedTo,
		&i.Resolution,
		&i.ResolvedAt,
		&i.ResolvedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const listComplaints = `-- name: ListComplaints :many
SELECT id, student_id, category, subject, body, priority, status, assigned_to, resolution, resolved_at, resolved_by, created_at, updated_at FROM complaints
ORDER BY created_at DESC
`

func (q *Queries) ListComplaints(ctx context.Context) ([]Complaint, error) {
	rows, err := q.db.Query(ctx, listComplaints)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Complaint{}
	for rows.Next() {
		var i Complaint
		if err := rows.Scan(
			&i.ID,
			&i.StudentID,
			&i.Category,
			&i.Subject,
			&i.Body,
			&i.Priority,
			&i.Status,
			&i.AssignedTo,
			&i.Resolution,
			&i.ResolvedAt,
			&i.ResolvedBy,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listStudentComplaints = `-- name: ListStudentComplaints :many
SELECT id, student_id, category, subject, body, priority, status, assigned_to, resolution, resolved_at, resolved_by, created_at, updated_at FROM complaints
WHERE student_id = $1
ORDER BY created_at DESC
`

func (q *Queries) ListStudentComplaints(ctx context.Context, studentID uuid.UUID) ([]Complaint, error) {
	rows, err := q.db.Query(ctx, listStudentComplaints, studentID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []Complaint{}
	for rows.Next() {
		var i Complaint
		if err := rows.Scan(
			&i.ID,
			&i.StudentID,
			&i.Category,
			&i.Subject,
			&i.Body,
			&i.Priority,
			&i.Status,
			&i.AssignedTo,
			&i.Resolution,
			&i.ResolvedAt,
			&i.ResolvedBy,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateComplaint = `-- name: UpdateComplaint :one
UPDATE complaints
SET
    category = $2,
    subject = $3,
    body = $4,
    priority = $5,
    status = $6,
    updated_at = NOW()
WHERE id = $1
RETURNING id, student_id, category, subject, body, priority, status, assigned_to, resolution, resolved_at, resolved_by, created_at, updated_at
`

type UpdateComplaintParams struct {
	ID       uuid.UUID         `json:"id"`
	Category ComplaintCategory `json:"category"`
	Subject  string            `json:"subject"`
	Body     string            `json:"body"`
	Priority ComplaintPriority `json:"priority"`
	Status   ComplaintStatus   `json:"status"`
}

func (q *Queries) UpdateComplaint(ctx context.Context, arg UpdateComplaintParams) (Complaint, error) {
	row := q.db.QueryRow(ctx, updateComplaint,
		arg.ID,
		arg.Category,
		arg.Subject,
		arg.Body,
		arg.Priority,
		arg.Status,
	)
	var i Complaint
	err := row.Scan(
		&i.ID,
		&i.StudentID,
		&i.Category,
		&i.Subject,
		&i.Body,
		&i.Priority,
		&i.Status,
		&i.AssignedTo,
		&i.Resolution,
		&i.ResolvedAt,
		&i.ResolvedBy,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}
